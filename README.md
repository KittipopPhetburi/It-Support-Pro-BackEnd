# ICDM Backend API

ระบบ Backend สำหรับจัดการ IT Service Management (ITSM) พัฒนาด้วย Laravel 11

## 🚀 Quick Start (วิธีเริ่มใช้งานสำหรับคนเพิ่งโหลดโปรเจกต์)

ทำตามขั้นตอนทีละข้อ รับรองรันผ่านแน่นอน!

### 1. เตรียมไฟล์ Environment
เมื่อโหลดโปรเจกต์มาครั้งแรก จะยังไม่มีไฟล์ `.env` ให้ก๊อปปี้จากตัวอย่าง:
```bash
cd backend
cp .env.example .env
```

### 2. ติดตั้ง Dependencies
ต้องลง Library ต่างๆ ของ PHP ก่อน (รันใน Terminal):
```bash
# ถ้ามี PHP ในเครื่อง
composer install

# หรือถ้าไม่มี PHP ให้ใช้ Docker ช่วยลง (ถ้าลง Docker ไว้แล้ว)
docker run --rm \
    -u "$(id -u):$(id -g)" \
    -v "$(pwd):/var/www/html" \
    -w /var/www/html \
    laravelsail/php82-composer:latest \
    composer install --ignore-platform-reqs
```

### 3. รันระบบด้วย Docker (แนะนำ 🏆)
วิธีนี้ง่ายสุด ไม่ต้องลง PHP/MySQL ในเครื่อง

1.  **Start Server**:
    ```bash
    docker-compose up -d
    ```
    *(รอสักพักจนกว่าจะนิ่ง)*

2.  **Generate Key** (ทำครั้งแรกครั้งเดียว):
    ```bash
    docker-compose exec app php artisan key:generate
    ```

3.  **สร้างตารางใน Database**:
    ```bash
    docker-compose exec app php artisan migrate --seed
    ```

---

## 🌐 การใช้งาน

*   **Web API**: `http://localhost`
*   **phpMyAdmin**: `http://localhost:8080` (User: `root`, Pass: `password`)

## 🧪 การทดสอบ (Testing)

รัน Unit/Feature Test:
```bash
docker-compose exec app php artisan test
```
